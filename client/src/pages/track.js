import React from "react";
import { gql, useQuery } from "@apollo/client";
import { Layout, QueryResult } from "../components";

//Functional componente Track getting trackId as parameter
const Track = ({ trackId }) => {
    return <Layout></Layout>;
}

export default Track;